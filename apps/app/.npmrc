# Use legacy peer deps to avoid peer dependency resolution failures during CI/Netlify installs
legacy-peer-deps=true
# Reduce noisy logs
loglevel=error
# Ensure node-gyp uses python3 where applicable
python=/usr/bin/python3
